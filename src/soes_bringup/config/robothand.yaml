soes_robothand:
  ros__parameters:
    rate_hz: 20.0
    pos_tol_m: 0.01
    settle_s: 0.5

    link_lengths_m: [0.00, 0.14, 0.12, 0.04]

    kp_cart: 1.0
    damping_lambda: 0.1
    qdot_limit_rad_s: [3.0, 3.0, 3.0, 3.0]

    q_min_rad: [-314.16, -157.08, -157.08, -1.5708]
    q_max_rad: [ 314.16,  157.08,  157.08,  1.5708]

    q_home_rad: [0.0, 0.10, -0.30, 0.20]
    kp_joint: 3.0
    home_tol_rad: 0.314

    # Legacy spiral parameters (left in place for backwards compatibility)
    R0: 0.025
    turns: 3
    alpha: -0.03
    height: 0.04
    omega: 2.0

    approach_duration_s: 1.5

    move_profile_time_s: 1.5   # slower / smoother move to cupcake
    home_profile_time_s: 1.5   # slower / smoother move back home

    # --- S-shape parameters (new) ---
    s_shape_R_m: 0.03
    s_shape_freq: 2
    s_shape_amp_scale: 0.95
    s_shape_n_points: 600
    s_shape_total_time_s: 8.0
    s_shape_enabled: true
