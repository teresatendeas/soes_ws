soes_vision:
  ros__parameters:

    # Camera settings
    camera_index: 0        # must match node.py (NOT camera_id)
    publish_rate_hz: 10.0  # rate at which detection runs and publishes
    frame_id: "camera_frame"

    # Visualization
    visualize: true        # show OpenCV debug window
